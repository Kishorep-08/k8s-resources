apiVersion: v1
kind: Service
metadata:
  name: service-demo
spec:
  selector:
    project: roboshop
    environment: dev
  ports:
  - protocol: TCP
    port: 80   # Service Port
    targetPort: 80  # Pod Container Port