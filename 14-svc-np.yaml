apiVersion: v1
kind: Service
metadata:
  name: svc-np
spec:
  selector:
    project: roboshop
    environment: dev
    purpose: nodeport
  type: NodePort
  ports:
  - port: 80 
    protocol: TCP
    targetPort: 80
    nodePort: 30010